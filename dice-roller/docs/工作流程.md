# DiceRoller 工作流程

本文说明如何把 `d:\GitHub\diceRoller` 的 GDScript 工程，分阶段移植到本 C# 工程，以及日后如何抽到其他 Godot C# 项目。架构与领域定义见 [项目文档.md](./项目文档.md)、[CONTEXT.md](../CONTEXT.md)、[adr/0001-face-slot-and-content.md](./adr/0001-face-slot-and-content.md)。

对照原则：**先对照原版跑通一个垂直切片，再扩功能。** 不要一次性翻译全部 35 个脚本。

---

## 1. 环境

| 工具 | 要求 |
|------|------|
| Godot | **4.7**，带 .NET（与 `project.godot` 的 `config/features` 一致） |
| SDK | .NET 8（Android 导出才需要 net9） |
| 对照用编辑器 | Godot **3.x**（打开源工程 `d:\GitHub\diceRoller\godot`） |
| IDE | Cursor / VS / Rider 均可；本仓库已用 `Directory.Build.props` 把 `obj/` 放在 csproj 旁，避免语言服务报找不到 `Godot` 命名空间 |

### 1.1 日常打开方式

1. 用 Godot 4.7 打开本目录（含 `project.godot` 的 `dice-roller`）。
2. 需要对照手感时，另开 Godot 3 打开 `d:\GitHub\diceRoller\godot`。源工程 Autoload 引用了缺失的 `debug_draw`，首次打开可能报错：可暂时从 `project.godot` 去掉 `DebugDraw` 这一行再运行（不要把该修改提交回源仓库，除非你维护的就是那份克隆）。
3. 在 Godot 中用 **构建** 编译 C#；不要只靠 `dotnet build` 却从未在编辑器里导入资源。

### 1.2 源与目标的关系

```
d:\GitHub\diceRoller\godot\     只读对照；不要在移植过程中改它来“顺便重构”
D:\GitHub\Godot-DiceRoller\     本 git 仓库根
  └── dice-roller\              Godot 4 C# 工程（工作区）
```

资源从源目录 **复制** 进来，脚本 **重写** 为 C#。不要在目标工程里 `symlink` 整个 Godot 3 项目。

---

## 2. 移植原则

1. **场景结构可抄，节点类型要升级。** `main.tscn` 的父子关系、Timer、Area 用途保留；`Spatial`→`Node3D` 等按第 4 节改。
2. **数据与节点分离。** FaceSlot 判定、FaceLayout、Session 放进普通 C# 类，Godot 节点只负责生命周期和场景。朝上结果必须是 `RollOutcome`，禁止把 `int 点数` 当成 Core 的唯一结果类型。
3. **禁止新的 `$"/root/main"`。** 用 `[Export]` 节点引用、组、或构造注入。这是“能迁到其他项目”的硬条件。
4. **壳体与含义分开抄。** 原 `dN.gd` 的向量只进 `DieFaceMap`；数字 1..N 只进 `FaceLayouts.StandardNumeric`。贴图与卡牌走 Presenter / Payload，不要改采样点。
5. **一次一个可玩切片。** 每阶段结束必须能在编辑器里点 Play 验证（见各阶段检查表）。
6. **行为分歧要写下来。** 物理引擎从 Bullet 换成 Jolt 后，弹跳和稳定时间不会逐帧相同。对齐的是规则（哪一槽朝上、何时 Invalid、热键），不是轨迹录像。
7. **GPL。** 拷贝原资源即继承 GPL-3.0。迁入闭源游戏前先看项目文档第 3.1 节。

---

## 3. 推荐目录演进

当前：可嵌入的物理骰插件（`addons/dice_roller/` + `plugin.cfg` Autoload）。`ImageFacePresenter` 按 `TextureKey` 叠 Decal。演示是左右栏 HUD，不是完整 Hundred Dice。设置窗 / Toast / Help / 原版 Theme / 指令语法 / HTML 导出 / 空白宿主验收 **不做**。最小场景与嵌入指南已完成。

移植过程中逐步变成：

```
dice-roller/
├── docs/
├── scenes/
│   ├── main.tscn                 演示应用（对标原 main.tscn）
│   └── test_headless_table.tscn  无 UI，只验证生成、RollOutcome 与 Layout.Replace
├── scripts/                      仅 App 入口，最终可删 Example
└── addons/dice_roller/           尽量早创建，避免以后大搬家
    ├── plugin.cfg                EditorPlugin：启用后注册 DiceSession / SettingsStore
    ├── Core/
    ├── Physics/
    ├── Presentation/
    ├── Session/
    ├── Settings/
    ├── Ui/
    └── assets/                   dice 场景 / 材质 / gltf；faces/example.png；world/wall.tscn
```

宿主拷贝 `addons/dice_roller/` 后启用插件即可注册 Autoload。无法用 EditorPlugin 时，仍可当运行时类库文件夹，按嵌入指南 §3.1 手填那两行。

---

## 4. Godot 3 / GDScript → Godot 4 / C# 对照

改脚本时按表替换，不要混用旧 API。

### 4.1 节点与资源

| Godot 3 | Godot 4 |
|---------|---------|
| `Spatial` | `Node3D` |
| `RigidBody` | `RigidBody3D` |
| `StaticBody` | `StaticBody3D` |
| `Area` | `Area3D` |
| `RayCast` | `RayCast3D` |
| `Camera` | `Camera3D` |
| `MeshInstance` | `MeshInstance3D` |
| `CollisionShape` | `CollisionShape3D` |
| `Viewport`（3D 预览） | `SubViewport` |
| `SpatialMaterial` | `StandardMaterial3D` |
| `StreamTexture` | `Texture2D` |
| `BoxShape.extents` | `BoxShape3D.Size`（全尺寸，不是半尺寸） |
| `translation` | `Position` |
| `Rect2` 控件 `rect_position` / `rect_size` | `Position` / `Size` |
| `pause_mode` | `ProcessMode` |
| `Tween` 节点 | `CreateTween()` |
| `File.new()` | `FileAccess` |
| `JSON.print` / `parse_json` | `Json.Stringify` / `Json.Parse` |
| `str2var` | 自己做颜色解析 |
| `yield(x, "timeout")` | `await ToSignal(...)` |
| `.instance()` | `.Instantiate<T>()` |
| `class_name Die` | `[GlobalClass] public partial class DieBody : RigidBody3D` |
| `tool` | `[Tool]` |
| `export var` | `[Export]` |
| `onready var x = $Mesh` | `_Ready` 里 `GetNode<MeshInstance3D>("Mesh")` 或 `[Export] NodePath` |
| `connect("sig", obj, "method")` | `SomeSignal += Handler` 或 `Callable` |
| `emit_signal("die_rolled", ...)` | `[Signal] public delegate void DieRolledEventHandler(...)` |
| `setget` | C# 属性 |
| `BUTTON_LEFT` | `MouseButton.Left` |
| `scancode` | `Key` / `Keycode` |
| `Input.CURSOR_DRAG` | `Input.CursorShape.Drag` |
| `shader_param` / `hint_color` | `SetShaderParameter` / `source_color` |
| `PoolStringArray` | `string[]` 或 `Godot.Collections.Array<string>` |
| Autoload 脚本 | 项目设置 Autoload + `public partial class SettingsStore : Node` |

### 4.2 物理与输入

- 刚体模式：`RigidBody.MODE_STATIC` → `RigidBody3D.FreezeMode` / `Freeze` + `FreezeModeStatic`（按 4.7 文档选用等价组合）。锁定角速度用 `AxisLockAngularX/Y/Z`。
- 射线：`cast_to` → `TargetPosition`；`collision_mask` 仍是位掩码，Dice = 层 2。
- `Area.SPACE_OVERRIDE_REPLACE` → `Area3D.SpaceOverride` 枚举。
- 重力点吸取：`GrabArea` 的 `gravity_point` / `linear_damp` 在 Jolt 下要实测；若吸力过弱或过强，只调 Area 参数，不要改判定规则。
- **BoxShape 尺寸陷阱：** Godot 3 `extents=(25,10,25)` 等于 Godot 4 `Size=(50,20,50)`。漏乘 2 会导致场地和生成范围错位。
- 地板碰撞半尺寸用于 `randomize_throw` 时，C# 必须用 **与 Godot 3 相同的半尺寸语义** 计算随机范围，或统一改成全尺寸并改公式。

### 4.3 C# 习惯

- 节点脚本必须是 `partial class`，且类名与文件名一致（Godot 约定）。
- 信号参数用强类型，不要 `Godot.Collections.Dictionary` 传递骰子状态。
- `SettingsData.listen(node, method)` 改为 `SettingsStore.Changed` 事件。
- `SettingsWindowDice` 里 `call("apply_" + setting)` 改为 `switch` 或 `Dictionary<string, Action<Color>>`。
- `instance_from_id`：优先在 `DieBody` 上保持托管引用；若必须用 id，用 `GodotObject.InstanceFromId` 并判空。
- 批量添加骰子的 `yield(timer, 0.0003)` 在 C# 用 `await ToSignal(GetTree().CreateTimer(0.0003), SceneTreeTimer.SignalName.Timeout)`，或按帧生成以免一帧刷几十个刚体。

### 4.4 着色器

`outline.shader` / `lock.shader` 从 Godot 3 spatial 迁到 4：

- `shader_type spatial;` 保留。
- `hint_color` → `source_color`。
- `hint_range` 语法核对其是否仍合法。
- 顶点膨胀描边在 Forward Plus 下可能与 GLES2 深度表现不同：无效面边线以“看得见”为准，不必像素级一致。

---

## 5. 分阶段流程

每阶段：复制或编写 → 编辑器运行 → 勾选检查表 → 再进入下一阶段。不要并行改 UI 和物理直到第 3 阶段通过。

### 阶段 0 — 工程能跑 3D

**做：**

- 删除或停用 `Example` 2D 占位（或留着但不作为主场景）。
- 新建 `scenes/main.tscn`：`Node3D` + `Camera3D` + `DirectionalLight3D` + 一块地板 `StaticBody3D`。
- `project.godot` 设置 `run/main_scene`、窗口 1440×900、stretch `canvas_items` + `expand`、物理层名称 Box / Dice。
- 注册输入动作（项目文档第 9 节），可先只加 `roll` 与鼠标。

**过关：** F5 看到 3D 地板，无 C# 编译错误。

### 阶段 1 — 资源进工程

**做：**

- 从源工程复制 `dice/gltf/`、`dice/materials/`、`dice/d*.tscn`、`Interface/Icons/`、`Interface/Fonts/`、`World/wall.tscn`。
- **不要**复制 `.import`、`.gd`、`addons/`。
- 用 Godot 4 打开，等待导入；修复报错的材质/着色器。
- 骰子 `.tscn`：去掉 GDScript，节点类型改为 `RigidBody3D` 等；暂时可没有脚本，只把一颗 d6 拖进主场景看网格和碰撞。

**过关：** 编辑器里能看见 d6 网格；运行时刚体掉在地板上（即使没有点数逻辑）。

**注意：** 骰子场景很大（内嵌 ArrayMesh）。用 Git 提交二进制前确认 `.gitattributes`。碰撞若在 4.x 中错位，再考虑从 `*collision.gltf` 重建，而不是一上来就重导视觉网格。

### 阶段 2 — 朝上判定与面内容（可单测）

**做：**

- `Core/DieFaceMap.cs`：HullKind、`FaceSlotId → Vector3`、阈值、`ReadUpSlot(Transform3D) → (IsValid, FaceSlotId)`。**返回槽位，不返回点数。**
- 把六个 `dN.gd` 的采样向量 **原样**抄进 FaceMap（含 d4 负向量）。
- `FaceContent`、`FaceLayout`、`RollOutcome`、`FaceLayouts.StandardNumeric(HullKind)`。
- 组合函数：`FaceReader.Read(map, layout, transform)` = 槽位判定 + Layout 查找。
- 单测：单位变换下 d6 朝上槽位对应标准 Layout 的 `NumericValue == 6`；斜放 `IsValid == false`；`layout.Replace` 后同一姿态读到新 `Id`。

**过关：** 固定姿态下六种壳体的槽位与原版脚本一致；改 Layout 不改判定；斜放为 Invalid。

此阶段不要写 UI，也不要写 Texture2D。数字只存在于 FaceContent 数据里。

**禁止：** `GetRolledSide() -> int` 作为公共接口（内部对照原版可留测试助手，但不要泄漏到 Physics/UI）。

### 阶段 3 — Die 节点 + Manager

**做：**

- `DieBody`：持有 FaceMap + 实例 FaceLayout；1 秒轮询发出 `RollOutcome`；Invalid 描边；锁定；掉落重生时 **复制当前 Layout**。
- `IFacePresenter`：`Bind` / `Apply` / `Unbind`（阶段 3 先挂空 `BakedNumeralPresenter`；换图见阶段 9）。
- `DiceManager`：按 `DieDefinition` 或 `SpawnStandard(hullKind)` 实例化、抛出、`Roll`。
- Autoload：`DiceSession`、`SettingsStore`（可先内存默认值，暂不写 JSON）。
- 无 UI：用 `_UnhandledInput` 按空格生成一颗标准数字 d6。

**过关：** 空格出骰，停下后能看到 RollOutcome（槽位与数字 Label）；推下悬崖再生后 Layout 仍在。

### 阶段 4 — 抓取交互

**做：** 移植 `DiceInteractions.gd` 的射线悬停、Y 平面拖拽、甩出上限、双击/W、右键/S 锁定与放下、`RotateTicks`。朝上对齐用 `FaceMap`，不用 `GetRolledSide() -> int`。

**过关（对照 Godot 3 原版，本阶段收窄）：**

- [x] 悬停高亮光标
- [x] 拖拽贴手，朝上面大致朝上
- [x] 甩出有速度上限
- [x] 双击 / W 随机抛
- [x] 右键锁定；锁定外观换材质
- [x] 拖到另一颗骰子上放下会叠高（4b）
- [x] Backspace 删除
- [x] 空白处吸取（4b）

**4b（已完成）：** `GrabArea` 吸取、`MinDistance`/`MaxDistance` 拖拽抬高、放下时向下射线叠放。Godot 4 的重力/阻尼覆盖是分开的，启用吸取时同时设 `GravitySpaceOverride` 与 linear/angular damp override 为 Replace。

Jolt 下若骰子更“粘”或更“弹”，只调摩擦/阻尼，不要改面判定阈值（阈值是几何量）。

### 阶段 5 — 场地与相机

**做：** 四面墙、顶、`global_area_scale`、滚轮缩放与改大小、Resize/Zoom Timer。不移植未挂载的 `camera.gd` 自由相机。

**过关：**

- [x] 四面墙 + 顶随 `Box` 缩放
- [x] Shift+滚轮改场地时骰子被唤醒
- [x] 场地约 `0.15 … 1.4`，步进 `0.02`
- [x] 相机距离夹在 4–60
- [x] 射线对墙体 `AddException`（导出 `OtherBoxSides`，不读 `$"/root/main"`）

### 阶段 6 — 统计与设置存储

**做：** `NumericScore`、Session 按 Id 计数、HUD 演示合计、`user://settings.json`、重力/颜色热更新。不做 Statistics 面板与设置三页。

**过关（本阶段收窄）：**

- [x] `DiceSession` 存 `RollOutcome`，不是 int
- [x] `NumericScore.Sum`：混合壳体相加；Invalid / 无 `NumericValue` 跳过
- [x] `CountByContentId("sword")`；Forget 后为 0
- [x] HUD 显示 count / sum / sword；F2 Replace 后立刻 `PollNow`
- [x] `SettingsStore` 读写 `user://settings.json`（颜色 HTML，不用 `str2var`）
- [x] 改重力立刻改 `GravityScale`；F4 改 `body_color` 立刻套共享材质
- [x] 右侧按 `HullKind` 分面统计（阶段 7 HUD，非原 `Statistics.tscn`）
- ~~设置窗口关窗保存~~ **不做**（`Set` 即写盘）

### 阶段 7 — 主 UI

按依赖从少到多：

1. [x] `DiceSelect` + `NumberInput` + 添加/清除/掷/锁按钮  
2. [x] 右侧 Session 统计 0.5s（按 `HullKind`，Invalid 为 `?`，sword 不加 sum）  
3. [x] 数字键 1–9、N、Space/R 全掷、Shift+Space 无效、L、C（只在 `DiceHud`）  
4. ~~Toast、版本消息~~ **不做**  
5. ~~Window / Settings 三页 / Help~~ **不做**  
6. ~~原版 Theme / IconButton / CheckToggle~~ **不做**（HUD 用默认控件 + `Interface/Icons/`）

**过关（已收窄）：** 不看 3D 也能靠左侧按钮加骰、清除、全掷、掷无效、锁有效面；右侧件数/合计/分面与 Session 一致；C 清场后统计消失。1–6 加对应壳体（7–9 落到 d20）。空格 / R 为 `RollAll`。根 UI `MouseFilter = Ignore`，中间仍能拖骰。

指令栏 / `4d6d1` **不做**。

### 阶段 8 — 复用形态

**做：**

- [x] 最小场景 `test_headless_table.tscn`：只有 Table API，没有 Interface。
- ~~拷到新建空白 Godot 项目做验收~~ **不做**（以嵌入指南 + 本仓库 headless 场景为准）。
- [x] 补一份短的「嵌入指南」：[嵌入指南.md](./嵌入指南.md)

**过关：** 不依赖 `scenes/main.tscn` 也能掷骰、读 RollOutcome、改面（见 `test_headless_table.tscn`）。

### 阶段 9 — 可选

- [x] `ImageFacePresenter`：按 `TextureKey`（`res://` 路径）给各 FaceSlot 叠 Decal。默认网格 UV 未按槽切开，故不用分面材质；不要改 `DieFaceMap`。用法见 [嵌入指南.md](./嵌入指南.md) §5.1.1。
- ~~指令语法模块~~ **不做**
- ~~HTML5 / 桌面导出预设~~ **不做**
- 用自有美术替换 GPL 资源（若宿主不能开源）——宿主自己的事，本仓库不排期。

---

## 6. 单次改动的日常循环

对每一个功能（例如“锁定”）：

1. **在 Godot 3 原版里操作一遍**，记下期望（热键、外观、边界情况）。
2. **只打开相关的原 `.gd` 与 `.tscn`**，列出信号与节点名。
3. **在 C# 写最小实现**，不顺手重构无关 UI。
4. **Play 对照** 项目文档第 6.3 节的交互表。
5. **修 Jolt / Godot 4 差异**（尺寸、冻结模式、着色器），直到规则一致。
6. 若发现原版 bug（例如 General 设置不恢复 `allow_locked_move`，重力默认 4 vs 滑条 fallback 10），在实现注释或本文件「已知原版缺陷」中记录你的选择：复现或修正。

提交（仅在被要求时）：按阶段或按功能提交，不要把“导入 200MB 网格”和“重写交互”混在一个 commit。

---

## 7. 资源复制清单（阶段 1 实用表）

从 `d:\GitHub\diceRoller\godot` 复制：

```
dice/gltf/
dice/d4.tscn
dice/d6.tscn
dice/d8.tscn
dice/d10.tscn
dice/d12.tscn
dice/d20.tscn
dice/materials/          # 含 shader 与 tres
World/wall.tscn
Interface/Icons/
Interface/Fonts/         # 含各字体 LICENSE
Interface/Themes/        # 阶段 7 再修兼容性
default_env.tres
icon.png                 # 可选，目标已有 icon.svg
```

不要复制：

```
*.gd
.import/
addons/
export_presets.cfg       # Godot 3 HTML5 预设
docs/（源仓库的 wasm 导出）
project.godot            # 不能覆盖 4.7 工程文件
```

场景复制后立即：更换节点类型、更换脚本引用、用 Godot 4 的信号连接 UI。

---

## 8. 迁入其他项目

步骤、拷贝清单、Autoload、物理层与示例代码以 **[嵌入指南.md](./嵌入指南.md)** 为准（给宿主程序员 / agent）。下面是摘要：

1. 拷贝 `addons/dice_roller/`（含 `assets/dice` 与 `assets/world/wall.tscn`）。
2. 启用插件 **DiceRoller**（写入 Autoload `DiceSession`、`SettingsStore`）；物理层 1=Box、2=Dice。
3. 实例化 `res://addons/dice_roller/DiceTable.tscn`，**不要**实例化演示 `scenes/main.tscn` / `DiceHud`，除非宿主也要那套 UI。
4. 代码只依赖 `IDiceTable` / `RollOutcome` / `FaceLayout`。订阅 `Rolled`，按 `Content.Id` 结算；`NumericScore.Sum` 可选。
5. 对照场景：`scenes/test_headless_table.tscn`。
6. 自定义图片：`TextureKey` + `ImageFacePresenter`（Decal）；不要改 `DieFaceMap`。见嵌入指南 §5.1.1。
7. GPL-3.0：见项目文档 §3.1。

输入动作名称建议与本仓库相同（`up`/`down`/`left`/`right`/`delete` 给抓取）；`roll` 等只在挂 `DiceHud` 时需要。

---

## 9. 验证清单（全量）

在宣称“可嵌入、演示可用”之前对照；未移植的原版 UI 铬不在此列。

### 骰子规则

- [x] d4–d20 每种壳体都能生成
- [x] 标准数字 Layout 下，平放槽位与原版采样表一致（每种抽查至少 3 槽）
- [x] 明显斜靠 → Invalid、描边、NumericScore 不加
- [x] `Layout.Replace` 后同一姿态读到新 Content.Id，刚体未销毁
- [x] 锁定换材质；`allow_locked_move` 开/关两种物理表现
- [x] `y < -4` 重生同 HullKind 且保留当前 FaceLayout（采样最高 Y 用 `-∞` 初值，骰子在世界原点以下仍能判定）

### 交互与快捷键

- [x] 左键拖拽投掷、双击抛、右键锁
- [x] W/S/A/D 与原版动作（含 left/right 对调）
- [x] Space/R 全掷，Shift+Space 只掷无效，L 锁有效面（再按解锁）
- [x] C 清场，N 添加当前类型，1–9 按类型索引添加
- [x] 滚轮缩放，Shift+滚轮改场地

### 数据

- [x] 演示统计合计、分面、删除后无幽灵 id
- [x] Session 主键不是裸 `int side`
- [x] `user://settings.json` 重启后恢复
- [x] 改 `body_color` / `number_color` / `border_color` 影响已有骰子

### 复用

- [x] 无 UI 场景可生成、读 RollOutcome、Replace 面内容
- ~~第二份空白工程仅靠插件文件夹工作~~ **不做**（见嵌入指南）
- [x] 无 `$"/root/main"` 字符串路径
- [x] Presenter 接口存在；换图不必改 DieFaceMap

### 工程卫生

- [x] 插件路径 `addons/dice_roller/` 大小写固定（Linux 友好）
- [x] 已移除对缺失 addons（debug_draw / dice_syntax）的引用
- [x] `Directory.Build.props` 仍保证 IDE 能解析 `Godot` 命名空间

---

## 10. 已知原版缺陷（移植时必须做选择）

| 现象 | 位置 | 建议 |
|------|------|------|
| `DebugDraw`、`dice_syntax` 缺失 | `project.godot` | C# 版不注册 |
| 指令输入无解析 | `CommandInput.gd` | **不移植** |
| `camera.gd` 未挂载 | `World/camera.gd` | 不移植 |
| `allow_locked_move` 不在 `apply_settings` 的 match 里 | `SettingsWindowGeneral.gd` | C# `SettingsStore` 读写该键；无设置窗 |
| 重力 fallback：Manager 4 vs 滑条 10 | `DiceManager.gd` / General | 统一默认 **4** |
| `roll()` 不解锁 STATIC 骰子 | `DiceManager.roll` | C# `Roll` 前 `SetLocked(false)` |
| HTML5 光标 Drag/CanDrop 对调 | `DiceInteractions.gd` | C# 已用 `OS.HasFeature("web")` 保留 |
| `Help.gd` 等出现在导出列表但不在源树 | `export_presets.cfg` | 忽略；Help **不移植** |
| 朝上采样 `highestY` 初值 0 | 各 `dN.gd` | 骰子整体在世界 Y=0 以下会得到 `Slot=None` 却仍 Valid；C# 用 `float.NegativeInfinity` |

把选择写在对应 C# 文件的简短注释里，避免以后“修回原版 bug”。

---

## 11. 风险

| 风险 | 缓解 |
|------|------|
| Jolt ≠ Bullet，落地更抖或更稳 | 只调物理材质与 `physics_ticks_per_second`；阈值不跟手感乱改 |
| Godot 4 导入打爆 Convex 碰撞 | 用 collision gltf 或简化凸包；d6 继续用盒子 |
| 内嵌网格导致仓库膨胀 | 接受，或改为运行时加载外部 gltf（工作量大，一期不建议） |
| `[Tool]` 控件在编辑器里跑 `_Process` | 未移植 IconButton / CheckToggle，无此问题 |
| 语言服务找不到 Godot 类型 | 不要删 `Directory.Build.props` |
| 原版网格数字烤在一张材质上，换图可能对不上 UV | 已用 Decal（`ImageFacePresenter`）；不要改 FaceMap |
| 闭源宿主误用 GPL 资源 | 嵌入前做一次资源来源审查 |

---

## 12. 文档维护

- 行为或模块边界变了：改 [项目文档.md](./项目文档.md) 与 [CONTEXT.md](../CONTEXT.md)。
- 槽位/内容关系动了：先改 ADR 或新增 ADR，再改项目文档。
- 阶段顺序、检查表、API 对照变了：改本文件。
- 不要把实现日记写进这两份文档；具体 bug 用 git / issue。
